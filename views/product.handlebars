<article class="post">
    <div class="title">
        <h2>{{post.title}}</h2>
        <h4>Posted by {{post.user.username}} on {{format_date post.created_at}}</h4>

        {{#if rating_score}}
        <h4>Rating: {{rating_score}}</h4>
        {{/if}}
    </div>
    <p>{{post.content}}</p>
</article>

{{#if loggedIn}}
<form class="product-form">
    <div>
        <textarea name="product-body"></textarea>
    </div>

    <div>
        <button type="-product-submit">add product description</button>
        <div class="ratingDropdown">
            <button onclick="myFunction()" class="userDropbtn">Dropdown</button>
            <div id="myDropdown" class="ratingDropdown-content">
                <a href="#">5 - Very good</a>
                <a href="#">4 - Pretty good</a>
                <a href="#">3 - Not bad</a>
                <a href="#">2 - Ok</a>
                <a href="#">1 - Terrible</a>
            </div>
        </div>
    </div>
    <div>
        <h2>Your location</h2>
    </div>
    <div id="mapid"></div>
</form>
{{/if}}

{{> comments post.comments}}

{{#if loggedIn}}
<script src="/javascript/comment.js"></script>
<script src="/javascript/user-rating.js"></script>
{{/if}}